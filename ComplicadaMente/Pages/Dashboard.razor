@page "/admin/dashboard"
@inject NavigationManager Navigation
@inject UserStateService UserState
@using Microsoft.AspNetCore.Components.Authorization

<PageTitle>Dashboard - ComplicadaMente</PageTitle>

<style>
  body {
    background-color: #cce7ff;
    height: 100vh;
    margin: 0;
    font-family: Arial, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .dashboard-container {
    background-color: white;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    width: 600px;
    display: flex;
    flex-direction: column;
    text-align: center;
  }

  h1 {
    color: #007acc;
    margin-bottom: 1rem;
    font-size: 2.5rem;
  }

  .subtitle {
    color: #333;
    margin-bottom: 2rem;
    line-height: 1.6;
    font-size: 1.1rem;
  }

  .dashboard-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
    margin-top: 20px;
  }

  .dashboard-card {
    background-color: #f8f9fa;
    border: 2px solid #007acc;
    border-radius: 10px;
    padding: 2rem;
    text-align: center;
    transition: all 0.3s ease;
    cursor: pointer;
    text-decoration: none;
    color: inherit;
  }

  .dashboard-card:hover {
    background-color: #007acc;
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0, 122, 204, 0.3);
    text-decoration: none;
  }

  .dashboard-card h3 {
    color: #007acc;
    margin-bottom: 10px;
    font-size: 1.3rem;
  }

  .dashboard-card:hover h3 {
    color: white;
  }

  .dashboard-card p {
    color: #666;
    font-size: 0.9rem;
    margin: 0;
  }

  .dashboard-card:hover p {
    color: #e6f3ff;
  }

  .icon {
    font-size: 2rem;
    margin-bottom: 10px;
    display: block;
  }

  .logout-btn {
    margin-top: 30px;
    padding: 10px 20px;
    background-color: #6c757d;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    transition: background-color 0.3s;
  }

  .logout-btn:hover {
    background-color: #545b62;
  }
</style>

<div class="dashboard-container">
    <h1>Dashboard Administrativo</h1>
    <p class="subtitle">Bem-vindo, @UserState.UserName!.</p>
    
    <div class="dashboard-grid">
        <a href="/admin/encomendas" class="dashboard-card">
            <span class="icon">📦</span>
            <h3>Encomendas</h3>
            <p>Visualizar e gerir todas as encomendas dos clientes</p>
        </a>

        <a href="/admin/quebracabecas" class="dashboard-card">
            <span class="icon">🧩</span>
            <h3>Quebra-Cabeças</h3>
            <p>Gerir catálogo de puzzles disponíveis</p>
        </a>

        <a href="/admin/pecas" class="dashboard-card">
            <span class="icon">🔧</span>
            <h3>Peças</h3>
            <p>Controlar inventário de peças individuais</p>
        </a>

        <a href="/admin/utilizadores" class="dashboard-card">
            <span class="icon">👥</span>
            <h3>Clientes</h3>
            <p>Visualizar e gerir utilizadores registados</p>
        </a>
    </div>
</div>

@code {
    protected override void OnInitialized()
    {
        // ensure only funcionarios can access this page
        if (!UserState.IsFuncionario)
        {
            Navigation.NavigateTo("/login", true);
        }
    }

    private void HandleLogout()
    {
        UserState.ClearUser();
        UserState.NotifyStateChanged();
        Navigation.NavigateTo("/", true);
    }
}